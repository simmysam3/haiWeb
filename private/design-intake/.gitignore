# Staging inbox for the generated design-system body (body.html) fed to the
# configuration-guide PDF build. Assembled per-release; never committed. The
# internal platform as-built is NOT staged here — it must not ship in the adopter PDF.
*
!.gitkeep
!.gitignore
