# dependencies
/node_modules
/.pnp
.pnp.*
__pycache__

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — match any .env variant (including backups like .env.foo.bak)
# so a stray secret-bearing file can never be committed by a bulk `git add`.
# The committed template is the one deliberate exception.
.env
.env.*
*.bak
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# legacy static site assets (moved to public/)
/img/
haiwave-site.zip

# Superpowers brainstorm session artifacts
.superpowers/

# Claude Code session-local state and worktrees
.claude/

# Playwright run artifacts
test-results/
playwright-report/
