export default function ReportsPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold text-charcoal mb-4">Audit Reports</h1>
      <p className="text-sm text-slate max-w-xl">
        Coming in Phase 8 — aggregate and per-vendor report rendering for your audit
        runs. Reports will be available as inline HTML, JSON export, and CSV download.
      </p>
    </div>
  );
}
