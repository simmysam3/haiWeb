export function ComplianceReservedFooter() {
  return (
    <p className="mt-6 text-xs text-slate italic">
      Compliance reporting fields (transformation chain, batch lineage,
      validity window) are reserved for a future release.
    </p>
  );
}
