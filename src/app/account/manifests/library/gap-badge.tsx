export function GapBadge() {
  return (
    <span
      className="inline-flex items-center rounded-full bg-warning/10 px-2 py-0.5 text-[11px] font-medium text-warning"
      title="This element has no document or value on file yet."
    >
      Needed
    </span>
  );
}
