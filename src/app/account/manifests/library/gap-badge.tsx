import { Pill } from '@/components/pill';

export function GapBadge() {
  return (
    <Pill tone="warn" definition="This element has no document or value on file yet.">
      Needed
    </Pill>
  );
}
