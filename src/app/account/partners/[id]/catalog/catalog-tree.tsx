'use client';

// Placeholder — full implementation in Task 30.
export function CatalogTree({ vendorId }: { vendorId: string }) {
  return (
    <div className="text-sm text-slate italic">
      Catalog tree for vendor {vendorId} — coming soon.
    </div>
  );
}
